# MAS AI代码审查系统 - 依赖包
# ⚠️ 以下「钉死版本」为论文复现实测版本，请勿随意升级大版本。
#    尤其 transformers / tokenizers / huggingface-hub / weaviate-client 升级会破坏 API 兼容。

# === 基础依赖 ===
click >= 8.1.7
loguru >= 0.7.2
pytest >= 7.4.3
httpx >= 0.25.2
python-dotenv >= 1.0.0

# === 核心AI/ML框架（钉死版本，升级会破坏兼容）===
transformers == 4.57.3        # Qwen1.5-7B-Chat 需 >=4.37；5.x 的 pipeline/AutoModel* API 已破坏
tokenizers == 0.22.1          # 必须与 transformers 4.57.3 匹配
huggingface-hub == 0.36.0     # transformers 4.57.3 要求 <1.0；1.x 会冲突
datasets == 4.4.1             # 本项目未直接 import，钉到实测版本避免被连带升级
accelerate == 1.12.0          # transformers device_map 依赖，钉到实测版本
safetensors >= 0.4.1
sentencepiece >= 0.1.99

# PyTorch - 深度学习框架
# ⚠️ torch 版本必须与 CUDA 驱动匹配：torch 2.9.x(cu126) 需要驱动 >= 560。
#    GPU 部署时优先复用平台预装 torch（venv 用 --system-site-packages），勿让 pip 升级；
#    若需重装，先 nvidia-smi 看 Driver Version，再选对应 cuXXX 轮子。
torch >= 2.1.1
# torchvision 本项目未 import，且安装会连带升级 torch，故注释掉：
# torchvision >= 0.16.0

# === 机器学习和数据科学 ===
scikit-learn >= 1.3.2
sentence-transformers == 5.1.2  # 本项目未直接 import，钉到实测版本
numpy >= 1.24.3, < 2.0          # torch 2.3.0 与 numpy 2.x 存在兼容性风险，锁 1.x 更稳
pandas >= 2.1.4, < 3.0          # pandas 3.x 有 copy-on-write 等破坏性变更

# === 数据库和存储 ===
sqlalchemy >= 2.0.23
aiosqlite >= 0.19.0
alembic >= 1.12.1
weaviate-client == 4.18.3       # 代码用 v4 API（connect_to_local/WeaviateClient），3.x 不兼容
# docker image: https://docker.aityp.com/image/docker.io/semitechnologies/weaviate:1.36.5

# === 代码分析工具 ===
pylint >= 3.0.3
bandit >= 1.7.5
flake8 >= 6.1.0

# === Git和版本控制 ===
gitpython >= 3.1.40

# === 异步和网络支持 ===
aiohttp >= 3.9.1
asyncio-mqtt >= 0.16.1

# === 配置和数据验证 ===
pydantic >= 2.5.1
pydantic-settings >= 2.1.0
jsonschema >= 4.20.0
pyyaml >= 6.0.1

# === 系统监控和性能 ===
psutil >= 5.9.6
watchdog >= 3.0.0

# === 用户界面和体验 ===
rich >= 13.7.0
tqdm >= 4.66.1
tabulate >= 0.9.0

# === 时间和数据处理 ===
python-dateutil >= 2.8.2
openpyxl >= 3.1.2

# === 类型支持和开发工具 ===
typing-extensions >= 4.8.0
pytest-asyncio >= 0.21.1
